name: taxi-pipeline

schedule: daily

start_date: "2021-01-01"

default_connections:
  duckdb: duckdb_default

variables:
  taxi_types:
    type: array
    items:
      type: string
    default: ["yellow", "green"]